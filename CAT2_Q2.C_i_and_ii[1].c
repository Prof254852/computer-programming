// An array is a data structure that stores a fixed 
// size sequence of elements all of the same time
#include <stdio.h>

int main() {
    // Declaring and initializing the 2D array
    int scores[2][2] = {{50, 65}, {90, 80}};
    
    return 0;
}